export enum Status {
    IN_PROGRESS = 'In Progress',
    COMPLETED = 'Completed',
    PENDING = 'Pending'
}

export enum Difficulty {
    EASY = 'Easy',
    MEDIUM = 'Medium',
    HARD = 'Hard'
}

export enum Priority {
    HIGH = 'High',
    MEDIUM = 'Medium',
    LOW = 'Low'
}